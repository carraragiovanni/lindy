const canvas = document.getElementById('pixelCanvas'); const ctx = canvas.getContext('2d'); function resizeCanvas() { canvas.width = window.innerWidth; canvas.height = window.innerHeight; } resizeCanvas(); window.addEventListener('resize', resizeCanvas); const pixels = []; for(let y = 0; y < canvas.height; y++) { for(let x = 0; x < canvas.width; x++) { pixels.push({x, y}); }} function shuffleArray(array) { for(let i = array.length - 1; i > 0; i--) { const j = Math.floor(Math.random() * (i + 1)); [array[i], array[j]] = [array[j], array[i]]; }} shuffleArray(pixels); function getRandomColor() { return `rgb(${Math.random()*256|0},${Math.random()*256|0},${Math.random()*256|0})`; } pixels.forEach((pixel, index) => { setTimeout(() => { ctx.fillStyle = getRandomColor(); ctx.fillRect(pixel.x, pixel.y, 1, 1); }, index);}); 