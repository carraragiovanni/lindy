function shuffle(arr){for(let i=arr.length-1;i>0;i--){const j=Math.floor(Math.random()*(i+1));[arr[i],arr[j]]=[arr[j],arr[i]];}}function getRandomColor(){return'#'+Math.floor(Math.random()*16777215).toString(16).padStart(6,'0');}const rows=50,cols=50;const gridEl=document.getElementById('grid');for(let i=0;i<rows*cols;i++){const pixel=document.createElement('div');pixel.className='pixel';gridEl.appendChild(pixel);}const pixelEls=document.querySelectorAll('.pixel');let indices=[...Array(pixelEls.length).keys()];shuffle(indices);let index=0;const interval=setInterval(function(){if(index>=pixelEls.length){clearInterval(interval);return;}pixelEls[indices[index]].style.backgroundColor=getRandomColor();index++;},1);